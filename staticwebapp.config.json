{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/*.css", "/*.js", "/assets/*", "/manifest.json", "/favicon.ico"]
  },
  "routes": [
    {
      "route": "/reporting",
      "rewrite": "/index.html"
    }
  ]
}
